<li>
  <%= link_to image_tag(screen.image.url(:thumb)), [@project, @feature, @flow, screen] %><br />
  <%= link_to "Delete", [@project, @feature, @flow, screen],
    :confirm => "Are you sure you want to delete this image?",
    :method => :delete %>
</li>